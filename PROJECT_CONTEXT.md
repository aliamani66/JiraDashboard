# 🤖 شناسنامه و زمینه فنی پروژه جهت بازیابی حافظه دستیار هوشمند (AI Memory Context)

> 📌 **توجه برای دستیار هوش مصنوعی (Antigravity AI):**  
> هنگام شروع گفتگو یا ادامه توسعه این پروژه، این فایل را جهت اطلاع از آخرین وضعیت پروژه، مسیرها، آدرس‌ها و خصوصیات سیستم مطالعه فرمایید.

---

## 📌 1. آدرس‌ها و مشخصات کلیدی پروژه

- **مسیر محلی دایرکتوری پروژه (Local Path):**  
  `C:\Users\USER\.gemini\antigravity\scratch\ops-showcase-dashboard`

- **آدرس مخزن گیت‌هاب (GitHub Repository URL):**  
  `https://github.com/aliamani66/JiraDashboard`  
  `https://github.com/aliamani66/JiraDashboard.git`

- **آدرس سرویس‌های در حال اجرا در محیط توسعه:**  
  - **Frontend (Vite + React 18):** `http://localhost:5173`
  - **Backend (Node.js + Express):** `http://localhost:3001`

- **اسکریپت‌های راه‌اندازی ۱ کلیکی:**  
  - ویندوز: `start.bat`  
  - لینوکس / مک: `start.sh`

---

## 🛠️ 2. پشته فن‌آوری و معماری سیستم (Tech Stack)

- **فرانت‌اند:** React 18، Vite 5، Framer Motion، Lucide React، Recharts، React Router v6، پشتیبانی کاملاً دارک‌مود و راست‌به‌چپ (RTL/فارسی).
- **بک‌اند:** Node.js 24، Express 4، SQLite 3 (`database.sqlite`)، Axios، JWT، bcryptjs، node-cron.
- **دیتابیس:** دیتابیس سبک و متمرکز SQLite 3 در فایل `backend/database.sqlite` به عنوان لایه بافر و کش داده‌ها.

---

## 🔑 3. اطلاعات ورود کاربران سیستم

| نام کاربری | کلمه عبور | نقش کاربری | سطوح دسترسی |
|------------|-----------|------------|--------------|
| **`admin`** | `admin123` | **مدیر ارشد سیستم (Admin)** | دسترسی کامل به تمام ۵ منو، مدیریت کاربران، تنظیمات دیتابیس و جیرا |
| **`waiting_user`** | `123456` | **بیننده تستی (Viewer)** | محدود به منوی **تسک‌های منتظر (`/waiting-tasks`)** با هدایت خودکار |

---

## 🚀 4. خلاصه‌سازی امکانات پیاده‌سازی‌شده در سامانه

### 🌐 ۱. داشبورد اصلی (`/`):
- کارت‌های آمار کل پروژه‌ها، در حال اجرا، انجام‌شده، میانگین ریاضی پیشرفت کل و تسک‌های منتظر.
- کارت پروژه‌ها با ارتفاع یکسان (Equal Height)، درصد درخشان، نوار تقدم انیمیشنی.
- فیلتر سریع کامپوننت‌ها و تب‌های وضعیت (`همه` | `در حال اجرا` | `انجام‌شده`).
- **هدر با پروفایل کاربر:** بج نام کاربر، بج درخشان نقش (`👑 مدیر ارشد سیستم` / `👤 کاربر داشبورد`) و منوی افتادنی تعاملی کاربر.

### 📋 ۲. جزئیات پروژه (`/project/:id`):
- بج‌های قابلیت‌های عملیاتی (Capabilities) با **لینک مستقیم به Confluence**.
- **تفکیک پویا کامپوننت‌ها:** با سقف ارتفاع ثابت ۱۷۰ پیکسل و اسکرول داخلی تیره.
- **گانت چارت چابک (Gantt Chart):** با **خط قرمز امروز (Today Line)**، لینک‌های مستقیم به Jira Cloud (`ORD-X`) و تولتیپ هوشمند.
- **بخش اسپرینت‌ها (Sprint Section):**
  - **نوار آمار تسک‌های مانده:** نمایش تعداد دقیق تسک‌های باقی‌مانده اسپرینت جاری (`⚡ ۴ تسک مانده`).
  - **تفکیک بار کاری اعضا:** چیپ‌های کوچک برای هر عضو پروژه و تعداد تسک‌های مانده‌اش با **فیلتر کلیکی روی اعضا**.

### 📈 ۳. تایم‌لاین پیشرفت کل (`/overall-timeline`):
- نمودار سرعت توسعه (Velocity Curve) چندپروژه‌ای Recharts و جدول مقایسه‌ای.

### ⏳ ۴. تسک‌های منتظر (`/waiting-tasks`):
- جدول تسک‌های معطل به تفکیک تیم منتظر (`waiting_for_team`) و علت انتظار (`waiting_reason`).

### 👥 ۵. مدیریت کاربران و دسترسی‌ها (`/user-management`):
- ساخت کاربر، تغییر کلمه عبور، تعیین نقش و **تخصیص دسترسی تک‌تک منوها به صورت تفکیک‌شده**.

### ⚙️ ۶. تنظیمات و پایش جیرا (`/jira-settings`):
- ویرایش و ذخیره زنده در فایل `backend/.env`.
- تعیین نسخه API (`auto` / `v3` / `v2`) و مسیرهای Endpoint.
- مدیریت پورت سرور و دیتابیس به همراه **دکمه همگام‌سازی و بازسازی زنده دیتابیس SQLite از Jira Cloud**.
- **ابزار «🔍 پایش زنده API»:** تست ساختار API جیرا با نمره تطابق ۱۰۰٪ و چاپ تمام `customfield_XXXXX`های فعال.
- **جدول نگاشت وضعیت‌ها (Status Mapping):** امکان تعریف وضعیت‌های جدید و سفارشی داشبورد.

### 📐 ۷. منوی جانبی (Sidebar):
- منوی جانبی در حالت بسته‌شده به **یک نوار باریک ۷۲ پیکسلی شکیل از آیکون‌ها با راهنمای Tooltip** تبدیل می‌شود.

---

## 📁 5. ساختار مهم‌ترین فایل‌های پروژه

```
ops-showcase-dashboard/
├── PROJECT_CONTEXT.md          # 👈 همین فایل شناسنامه و حافظه دستیار هوشمند
├── DOCUMENTATION.md            # مستندات جامع کاربری و فنی سامانه
├── README.md                   # راهنمای سریع پروژه در گیت‌هاب
├── start.bat                   # اسکریپت راه‌اندازی ۱ کلیکی ویندوز
├── start.sh                    # اسکریپت راه‌اندازی ۱ کلیکی لینوکس/مک
├── backend/
│   ├── .env                    # فایل اصلی تنظیمات و کلیدهای Jira/Confluence/DB
│   ├── database.sqlite         # فایل متمرکز دیتابیس SQLite
│   ├── src/
│   │   ├── app.js              # سرور اصلی بک‌اند (Port 3001)
│   │   ├── jiraMapping.js      # نگاشت پیش‌فرض وضعیت‌ها و فیلدها
│   │   ├── routes/             # روت‌های auth, projects, users, jiraSettings, sync
│   │   └── services/           # سرویس‌های jiraService, cacheService, authService
└── frontend/
    ├── src/
    │   ├── App.jsx             # مسیریابی اصلی فرانت‌اند (Port 5173)
    │   ├── services/api.js     # سرویس فراخوانی APIها
    │   ├── context/AuthContext.jsx # مدیریت نشست کاربر
    │   ├── components/Layout/  # Sidebar.jsx (72px narrow), Layout.jsx
    │   └── pages/              # DashboardPage, ProjectPage, JiraSettingsPage, etc.
```

---

## 📌 6. راهنمای بازیابی سریع برای گفتگوهای بعدی با AI

در گفتگوهای آینده، اگر خواستید توسعه جدیدی روی این پروژه انجام دهید، کافیست بگویید:
> **«فایل `PROJECT_CONTEXT.md` را بخوان و ادامه پروژه را انجام بده»**

دستیار هوشمند فوراً با خواندن این فایل، تمام جزئیات، آدرس‌ها، دیتابیس، ساختار فرانت‌اند/بک‌اند و تنظیمات پروژه شما را مسلط خواهد بود. 🚀

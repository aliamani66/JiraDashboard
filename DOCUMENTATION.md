# 📘 مستندات جامع سامانه داشبورد عملیات R&D (Jira Operations Showcase Dashboard)

این سند شامل راهنمای جامع معماری، نصب و راه‌اندازی ۱ کلیکی، نحوه اتصال به Jira/Confluence، **مستندات تفصیلی تمام صفحات و بخش‌های کاربری UI** و **مستندات کامل بخش تنظیمات و نگاشت داده‌ها** می‌باشد.

---

## 📑 فهرست مطالب

۱. [معماری و ویژگی‌های کلی سامانه](#1-معماری-و-ویژگی‌های-کلی-سامانه)  
۲. [راهنمای نصب و راه‌اندازی ۱ کلیکی](#2-راهنمای-نصب-و-راه‌اندازی-۱-کلیکی)  
۳. [راهنمای جامع کاربری و صفحات داشبورد (UI / User Interface Guide)](#3-راهنمای-جامع-کاربری-و-صفحات-داشبورد)  
۴. [مستندات کامل بخش تنظیمات و پایش جیرا (/jira-settings)](#4-مستندات-کامل-بخش-تنظیمات-و-پایش-جیرا)  
۵. [پیکربندی کامل فایل .env](#5-پیکربندی-کامل-فایل-env)  

---

## 1. معماری و ویژگی‌های کلی سامانه

سامانه ویترین عملیاتی R&D یک داشبورد مدرن، زیبا و دارک‌مود بر پایه معماری لایه‌ای زیر می‌باشد:

- **فرانت‌اند (Frontend):** توسعه داده‌شده با React 18، Vite، Framer Motion، Lucide React، Recharts و پشتیبانی کامل از RTL/فارسی.
- **بک‌اند (Backend):** سرویس‌دهنده Node.js / Express با معماری RESTful API.
- **دیتابیس (Database):** دیتابیس سبک و متمرکز SQLite 3 (`database.sqlite`) که نقش لایه بافر و کش داده‌های همگام‌شده را ایفا می‌کند.
- **احراز هویت و دسترسی (Auth & ACL):** سیستم احراز هویت توکن‌محور JWT همراه با رمزنگاری bcryptjs و جدول کنترل دسترسی منومحور (Permission-based ACL).

---

## 2. راهنمای نصب و راه‌اندازی ۱ کلیکی

### 🔹 در ویندوز (Windows)
کافیست در ریشه پروژه روی فایل **`start.bat`** دابل‌کلیک نمایید. اسکریپت به صورت خودکار:
۱. وابستگی‌های بک‌اند و فرانت‌اند (`npm install`) را بررسی و نصب می‌کند.
۲. دیتابیس اولیه را ایجاد می‌نماید.
۳. بک‌اند (پورت ۳۰۰۱) و فرانت‌اند (پورت ۵۱۷۳) را اجرا می‌کند.
۴. مرورگر را روی آدرس **`http://localhost:5173`** باز می‌کند.

### 🔹 در لینوکس / مک (Linux / Mac)
```bash
chmod +x start.sh
./start.sh
```

### 🔑 حساب‌های کاربری پیش‌فرض سیستم:
- **مدیر ارشد سیستم (Admin):**  
  نام کاربری: `admin` | کلمه عبور: `admin123`
- **کاربر تستی (محدود به تسک‌های منتظر):**  
  نام کاربری: `waiting_user` | کلمه عبور: `123456`

---

## 3. راهنمای جامع کاربری و صفحات داشبورد (UI / User Interface Guide)

در این بخش، تمامی صفحات، کارت‌ها، نمودارها، ابزارها و قابلیت‌های کاربری فرانت‌اند به تفصیل توضیح داده شده‌اند.

---

### 🌐 ۳.۱. صفحه اصلی داشبورد (Dashboard Home Page - `/`)

صفحه اصلی داشبورد نما و ویترین کلی تمام پروژه‌های R&D سازمان است.

#### 🔹 نوار بالای صفحه (Topbar):
- **عنوان سامانه:** «داشبورد ویترین عملیات R&D».
- **دکمه منوی کشویی (Menu Toggle):** باز و بسته کردن منوی جانبی.
- **نمایش زمان آخرین سینک:** نمایش دقیق زمان آخرین همگام‌سازی موفق با جیرا (مثلاً `آخرین سینک: ساعت ۰۰:۰۵`).
- **دکمه همگام‌سازی زنده (`🔄`):** اجرای فرایند همگام‌سازی دستی با جیرا در لحظه به همراه آیکون چرخنده و پیغام Toast.
- **پروفایل تعاملی کاربر:**
  - نمایش نام نمایشی کاربر (مثلاً `مدیر سیستم` یا `علی امانی`).
  - بج رنگی نقش کاربر (`👑 مدیر ارشد سیستم` به رنگ سبز یا `👤 کاربر داشبورد` به رنگ آبی).
  - **منوی افتادنی کاربر (User Dropdown Card):** با کلیک روی پروفایل باز می‌شود و شامل نام کامل، آیدی سیستم (`@admin`)، نقش و دکمه **`🚪 خروج از حساب کاربر`** می‌باشد.

#### 🔹 کارت‌های آمار کلی (Stats Cards):
- 📂 **تعداد کل پروژه‌ها:** مجموع پروژه‌های تعریف‌شده در جیرا.
- ⚡ **پروژه‌های در حال اجرا:** تعداد پروژه‌های اکتیو.
- ✅ **پروژه‌های انجام‌شده:** تعداد پروژه‌هایی که تمام تسک‌های آنها تکمیله (`status = Done`).
- 📈 **میانگین پیشرفت کل:** میانگین ریاضی درصد پیشرفت تمام پروژه‌ها. *(با کلیک روی این کارت به صفحه تایم‌لاین پیشرفت کل هدایت می‌شوید).*
- ⏳ **تسک‌های منتظر:** مجموع کل تسک‌هایی که در تمام پروژه‌ها معطل سایر تیم‌ها هستند. *(با کلیک روی این کارت به صفحه تسک‌های منتظر هدایت می‌شوید).*

#### 🔹 فیلترهای بالای لیست پروژه‌ها:
- **تب‌های وضعیت پروژه:**
  - `🌐 همه پروژه‌ها` (نمایش تمام پروژه‌ها)
  - `⚡ در حال اجرا` (حذف پروژه‌های کامل‌شده)
  - `✅ انجام‌شده` (فقط پروژه‌های پایان یافته)
- **دکمه‌های فیلتر سریع کامپوننت‌ها (Component Filter Buttons):**  
  فیلتر آنی پروژه‌ها بر اساس فعالیت‌ها (مانند `یادگیری/R&D` ، `جلسات` ، `پشتیبانی/عملیات` یا `همه`).

#### 🔹 شبکه‌ای از کارت پروژه‌ها (Equal-Height Project Cards Grid):
- **کارت پروژه‌ها:** با ارتفاع یکسان (Equal Height)، نام پروژه، کد پروژه در جیرا (لینک به Jira Epic)، درصد پیشرفت درخشان، نوار تقدم انیمیشنی، تعداد تسک‌های انجام‌شده و بج وضعیت پروژه.
- **دکمه «مشاهده جزئیات پروژه ➔»:** وارد شدن به صفحه اختصاصی همان پروژه.

---

### 📋 ۳.۲. صفحه جزئیات پروژه (Project Details Page - `/project/:id`)

صفحه تخصصی تحلیل و مدیریت هر پروژه عملیاتی.

#### 🔹 هدر پروژه (Project Header):
- عنوان پروژه، کد Jira Epic، توضیحات کامل استخراج‌شده از جیرا.
- **بج‌های قابلیت‌های عملیاتی (Capabilities):** بج‌های رنگی نشان‌دهنده توانمندی‌های پروژه به همراه **لینک مستقیم به فضای Confluence**.
- نوار پیشرفت بزرگ و درصد واقعی تکمیل پروژه.

#### 🔹 تفکیک پویا کامپوننت‌ها (Component Breakdown Section):
- محاسبه درصد سهم و ساعات صرف‌شده/زمان‌بندی‌شده برای هر کامپوننت در پروژه (از طریق فیلد نیتیو جیرا یا برچسب‌های `comp:`).
- **طراحی بهینه UI:** دارای سقف ارتفاع ثابت ۱۷۰ پیکسل با اسکرول داخلی تیره تا صفحه بیش از حد طولانی نشود.

#### 🔹 گانت چارت چابک (Agile Gantt Chart):
- نمایش نوار زمانی تسک‌های پروژه در طول هفته‌ها و اسپرینت‌ها.
- **خط قرمز امروز (Today Line):** یک خط متحرک قرمز رنگ که موقعیت دقیقی از روز جاری را روی گانت چارت نشان می‌دهد.
- **لینک‌های مستقیم به Jira Cloud:** هر تسک روی گانت چارت دارای شناسه کلیکی (مانند `ORD-12`) با آیکون ExternalLink است که کاربر را مستقیماً به تسک مربوطه در Jira Cloud منتقل می‌کند.
- **تولتیپ هوشمند (Hover Tooltip):** با نگه داشتن ماوس روی هر نوار، نام مسئول، ساعات تخمینی/صرف‌شده و تاریخ شروع/پایان نمایش داده می‌شود.

#### 🔹 بخش اسپرینت‌ها (Sprint Section):
- **منوی انتخاب اسپرینت (Sprint Selector):** منوی افتادنی جهت سوئیچ بین اسپرینت‌ها (اسپرینت جاری به صورت پیش‌فرض در بالا قرار دارد).
- **نوار آمار تسک‌های مانده (Sprint Stats Banner):**
  - ⚡ **تسک‌های باقی‌مانده و انجام‌نشده:** تعداد تسک‌های مانده اسپرینت جاری با بج درخشان نارنجی (مثلاً `۴ تسک مانده`).
  - ✅ **تکمیل‌شده:** تعداد تسک‌های Done شده از کل (مثلاً `۷ از ۱۱`).
  - ⏳ **معطل تیم‌های دیگر:** تسک‌های در انتظار.
- **تفکیک بار کاری اعضا (Assignees Workload Breakdown):**
  - چیپ‌های کوچک برای هر یک از اعضای پروژه در اسپرینت جاری.
  - نمایش تعداد تسک‌های مانده هر نفر (مانند `مریم ابراهیمی: ⚡ ۴ مانده` یا `ساره حسینی: ⚡ ۳ مانده`).
  - **فیلتر کلیکی روی اعضا:** با کلیک روی نام هر شخص، لیست تسک‌های اسپرینت فوراً فیلتر شده و فقط تسک‌های همان شخص نمایش داده می‌شود.

---

### 📈 ۳.۳. صفحه تایم‌لاین پیشرفت کل (`/overall-timeline`)

- **دکمه بازگشت سریع به داشبورد:** جهت ناوبری آسان کاربر.
- **نمودار سرعت توسعه (Velocity Curve Chart):** نمودار ترکیبی Recharts برای مقایسه منحنی پیشرفت تمام پروژه‌ها در یک نگاه.
- **جدول مقایسه‌ای پروژه‌ها:** نمایش نام، کلید جیرا، وضعیت، تعداد تسک‌ها، درصد پیشرفت و بازه زمانی کل پروژه.

---

### ⏳ ۳.۴. صفحه تسک‌های منتظر (`/waiting-tasks`)

- مخصوص پایش تسک‌هایی که معطل سایر تیم‌ها (مانند زیرساخت، امنیت، محصول) هستند.
- **کارت‌های آمار تفکیکی:** مجموع تسک‌های منتظر و تعداد تیم‌های معطل‌کننده.
- **جدول تسک‌های معطل:** شناسه و عنوان تسک با لینک به Jira Cloud، نام تیمی که تسک منتظر آن است (`waiting_for_team`)، علت دقیق انتظار (`waiting_reason`)، نام پروژه و مسئول تسک.

---

### 👥 ۳.۵. صفحه مدیریت کاربران و سطوح دسترسی (`/user-management`)

- **ایجاد کاربر جدید:** فرم تعریف نام کاربری، کلمه عبور، نام نمایشی و تعیین نقش (`admin` / `viewer`).
- **تخصیص دسترسی منوها (Menu-level Access Control):**
  - چک‌باکس‌های اختصاصی برای تعیین اینکه کاربر به کدام یک از منوها دسترسی داشته باشد:
    - 🌐 داشبورد اصلی
    - 📈 تایم‌لاین پیشرفت کل
    - ⏳ تسک‌های منتظر
    - 👥 مدیریت کاربران
    - ⚙️ تنظیمات و پایش جیرا
- **ویرایش و حذف کاربران:** امکان ویرایش دسترسی‌های کاربر یا حذف حساب (با محافظت از حساب `admin`).

---

### 📐 ۳.۶. نوار ناوبری جانبی (Sidebar)

- **حالت باز (Expanded):** نمایش کامل عنوان سامانه، لوگو، متون منوها و پروفایل کاربر.
- **حالت بسته‌شده / آیکونی (Collapsed 72px Icon Bar):**  
  با زدن دکمه `<` بالای منو، منو به یک نوار باریک ۷۲ پیکسلی شکیل تبدیل می‌شود که فقط آیکون‌های متمرکز صفحات به همراه راهنمای Tooltip را نشان می‌دهد تا فضای زیادی برای مشاهده داشبورد و نمودارها فراهم گردد.

---

## 4. مستندات کامل بخش تنظیمات و پایش جیرا (`/jira-settings`)

این بخش مهم‌ترین مرکز کنترل سامانه است که به شما امکان متصل کردن هر نوع جیرایی (Cloud / Server / Data Center) را بدون نیاز به تغییر کد می‌دهد.

### ⚙️ اولویت تنظیمات:
تنظیمات سیستم در فایل **`backend/.env`** نگهداری می‌شوند. هر تغییری که در پنل وب ثبت و ذخیره کنید، **مستقیماً داخل فایل `.env` بک‌اند نوشته می‌شود.**

---

### 📊 جدول ماتریس اثرگذاری فیلدهای تنظیمات بر بخش‌های داشبورد (Field-to-Feature Impact Matrix)

جدول زیر دقیقاً نشان می‌دهد که **هر فیلد تنظیمات مستقیماً روی کدام صفحه، نمودار یا کامپوننت فرانت‌اند اثر می‌گذارد:**

| نام فیلد در تنظیمات | کلید در فایل `.env` | تاثیر مستقیم روی کدام بخش از برنامه؟ | نحوه و محل اثرگذاری دقیق |
|---------------------|---------------------|-------------------------------------|---------------------------|
| **آدرس پایه Jira (Base URL)** | `JIRA_BASE_URL` | • تمام لینک‌های `ORD-X` در گانت‌چارت و تسک‌ها<br>• سرویس همگام‌سازی (`jiraService.js`) | آدرس پایه تمام لینک‌های کلیکی تسک‌ها جهت انتقال کاربر به Jira Cloud و دامنه اتصال API. |
| **نام کاربری / ایمیل** | `JIRA_USERNAME` | • احراز هویت با Jira API | احراز هویت دسترسی به API جیرا. |
| **API Token / کلمه عبور** | `JIRA_TOKEN` | • احراز هویت با Jira API | مجوز توکن یا کلمه عبور دسترسی به API جیرا. |
| **کلید پروژه اصلی** | `JIRA_PROJECT_KEY` | • داشبورد اصلی (`/`)<br>• گانت چارت و اسپرینت‌ها | سیستم فقط پروژه‌ها و تسک‌های مربوط به این کلید در جیرا را استخراج می‌کند. |
| **فاصله سینک (دقیقه)** | `SYNC_INTERVAL_MINUTES` | • کرون‌جاب پس‌زمینه (`cacheService.js`) | زمان‌بندی تکرار خودکار همگام‌سازی دیتابیس با جیرا در پس‌زمینه. |
| **نوع و نسخه Jira API** | `JIRA_API_VERSION` | • لایه ارتباطی API (`jiraService.js`) | تعیین فرمت متدهای API (Cloud v3 یا Server v2). |
| **Endpoint جستجو** | `JIRA_SEARCH_ENDPOINT` | • دریافت تسک‌ها در همگام‌سازی | مسیر اجرای پرس‌وجوهای JQL در جیرا. |
| **Endpoint پروژه** | `JIRA_PROJECT_ENDPOINT` | • دریافت مشخصات پروژه | مسیر استخراج متادیتای پروژه‌ها در جیرا. |
| **پورت سرور (Port)** | `PORT` | • سرویس‌دهنده بک‌اند (`app.js`) | پورتی که سرور Node.js روی آن اجرا می‌شود (پیش‌فرض: ۳۰۰۱). |
| **آدرس Base Confluence** | `CONFLUENCE_BASE_URL` | • بج‌های قابلیت‌ها (Capabilities) در پروژه | تمام کلیک‌ها روی بج‌های قابلیت‌ها کاربران را مستقیماً به این آدرس Confluence منتقل می‌کنند. |
| **کلید پیش‌فرض Space** | `CONFLUENCE_DEFAULT_SPACE` | • بج‌های قابلیت‌ها | فضای کاری مستندات فنی در Confluence. |
| **وضعیت‌های منتظر** | `JIRA_WAITING_STATUSES` | • صفحه تسک‌های منتظر (`/waiting-tasks`)<br>• کارت‌های آمار داشبورد | تسک‌هایی که این وضعیت‌ها را دارند در لیست «تسک‌های منتظر» دسته‌بندی می‌شوند. |
| **نگاشت وضعیت‌ها (Status Mapping)** | `statusMapping` | • بج‌های وضعیت (`StatusBadge.jsx`)<br>• محاسبات پیشرفت پروژه‌ها<br>• تب‌های فیلتر داشبورد | تعیین می‌کند کدام وضعیت انجام‌شده (Done) به حساب بیاید تا درصد پیشرفت پروژه‌ها محاسبه شود. |
| **فیلد Sprint** | `JIRA_SPRINT_FIELD` | • گانت چارت چابک (`GanttChart.jsx`)<br>• بخش اسپرینت‌ها (`SprintSection.jsx`) | استخراج اطلاعات اسپرینت‌ها جهت گروه‌بندی تسک‌ها در اسپرینت‌های چابک. |
| **فیلد تیم منتظر** | `JIRA_WAITING_TEAM_FIELD` | • جدول تسک‌های منتظر (`/waiting-tasks`) | نمایش نام تیمی که تسک معطل آن است. |
| **فیلد دلیل انتظار** | `JIRA_WAITING_REASON_FIELD` | • جدول تسک‌های منتظر (`/waiting-tasks`) | نمایش علت دقیق معطل ماندن تسک. |
| **فیلد لینک Confluence** | `JIRA_CONFLUENCE_LINK_FIELD` | • بج‌های قابلیت‌ها در پروژه | لینک اختصاصی سند Confluence برای هر اپیک یا تسک. |
| **پیشوند برچسب تیم (`wait:`)** | `JIRA_WAIT_TEAM_PREFIX` | • صفحه تسک‌های منتظر | استخراج نام تیم معطل‌کننده از برچسب‌های تسک (مثلاً `wait:infra`). |
| **پیشوند دلیل انتظار (`reason:`)** | `JIRA_WAIT_REASON_PREFIX` | • صفحه تسک‌های منتظر | استخراج علت انتظار از برچسب‌های تسک (مثلاً `reason:approval`). |
| **پیشوند قابلیت (`cap:`)** | `JIRA_CAPABILITY_PREFIX` | • هدر صفحه پروژه (`ProjectHeader.jsx`) | استخراج قابلیت‌های عملیاتی پروژه از برچسب‌ها (مثلاً `cap:monitoring`). |
| **کامپوننت‌های برجسته** | `JIRA_FEATURED_COMPONENTS` | • فیلترهای بالای داشبورد اصلی (`/`) | ساخت دکمه‌های فیلتر سریع در بالای صفحه اصلی داشبورد. |

---

### 🌐 ۴.۱. اتصال به Jira (Connection Settings)

| نام فیلد در UI | کلید در فایل `.env` | توضیحات و نحوه دریافت |
|-----------------|---------------------|----------------------|
| **آدرس پایه Jira** | `JIRA_BASE_URL` | آدرس دامنه جیرای شما. مثال Cloud: `https://company.atlassian.net` — مثال Server: `https://jira.company.com` |
| **نام کاربری / ایمیل** | `JIRA_USERNAME` | ایمیل حساب Atlassian در نسخه Cloud (مثال: `user@company.com`) یا نام کاربری لاگین در نسخه Server. |
| **API Token / کلمه عبور** | `JIRA_TOKEN` | در Jira Cloud: توکن API تولید شده از مسیر `id.atlassian.com/manage-profile/security/api-tokens`. در Jira Server: کلمه عبور یا Personal Access Token. |
| **کلید پروژه اصلی** | `JIRA_PROJECT_KEY` | شناسه ۳ یا ۴ حرفی پروژه اصلی در جیرا (مثال: `ORD` یا `OPS`). |
| **فاصله سینک (دقیقه)** | `SYNC_INTERVAL_MINUTES` | بازه زمان‌بندی همگام‌سازی خودکار بک‌اند در پس‌زمینه (پیش‌فرض: `60` دقیقه). |

---

### 🖥️ ۴.۲. نسخه و مسیرهای API جیرا (API Version & Endpoints)

| نام فیلد در UI | کلید در فایل `.env` | مقادیر معتبر | توضیحات |
|-----------------|---------------------|--------------|----------|
| **نوع و نسخه Jira API** | `JIRA_API_VERSION` | `auto` , `v3` , `v2` | در حالت `auto` سیستم ابتدا نسخه REST API v3 جیرای کلاد را تست کرده و در صورت دریافت error 404 خودکار سوئیچ به API v2 جیرای سرور می‌کند. |
| **Endpoint جستجو** | `JIRA_SEARCH_ENDPOINT` | `/rest/api/3/search/jql` | مسیر متد اجرای پرس‌وجوهای JQL در جیرا. |
| **Endpoint پروژه** | `JIRA_PROJECT_ENDPOINT` | `/rest/api/3/project` | مسیر متد دریافت مشخصات پروژه در جیرا. |

---

### 💾 ۴.۳. تنظیمات سرور و دیتابیس (Server & DB Management)

- **پورت سرور (`PORT`):** پورت سرویس بک‌اند (پیش‌فرض: `3001`).
- **کلید امنیتی (`JWT_SECRET`):** عبارت رمزی جهت امضای توکن‌های نشست کاربران.
- **وضعیت دیتابیس:** نمایش وضعیت فایل `database.sqlite`.
- **دکمه `🔄 همگام‌سازی و بازسازی دیتابیس از Jira Cloud`:**  
  با زدن این دکمه، دیتابیس محلی پاکسازی شده و تمام پروژه‌ها و تسک‌ها زنده و ۱۰۰٪ بر اساس داده‌های موجود در Jira Cloud بازسازی می‌شوند.

---

### 📚 ۴.۴. اتصال به Confluence (Confluence Settings)

| نام فیلد در UI | کلید در فایل `.env` | توضیحات |
|-----------------|---------------------|----------|
| **آدرس Base Confluence** | `CONFLUENCE_BASE_URL` | آدرس پایگاه مستندات سازمان (مثال: `https://company.atlassian.net/wiki`). |
| **ایمیل / نام کاربری** | `CONFLUENCE_USERNAME` | ایمیل حساب دسترسی به کانفلوئنس. |
| **کلید پیش‌فرض Space** | `CONFLUENCE_DEFAULT_SPACE` | کلید فضای کاری مستندات در کانفلوئنس (مثال: `OPS` یا `TECH`). |

---

### ⏳ ۴.۵. وضعیت‌های منتظر (Waiting Status List)

وضعیت‌هایی از تسک در جیرا که نشان‌دهنده معطل ماندن کار روی سایر تیم‌ها هستند.
- **کلید `.env`:** `JIRA_WAITING_STATUSES`
- **مقادیر پیش‌فرض:** `OnHolding, Waiting, Blocked, On Hold`
- **نحوه ورود در UI:** وارد کردن عنوان وضعیت و فشردن دکمه Enter جهت تبدیل به تگ.

---

### 🟢 ۴.۶. نگاشت وضعیت‌های جیرا به داشبورد (Status Mapping)

در جدول **Status Mapping**، تمام وضعیت‌های اولیه در جیرا به وضعیت‌های استاندارد یا سفارشی داشبورد تبدیل می‌شوند:

```
[نام وضعیت در Jira] ───► [نام وضعیت در داشبورد]
   'Closed'          ───►    'Done' (انجام شده)
   'In Development'  ───►    'In Progress' (در حال انجام)
   'Hold by Infra'   ───►    'Waiting' (در انتظار)
   'Code Review'     ───►    'In Review' (وضعیت سفارشی جدید)
```

> ✏️ **ایجاد وضعیت سفارشی جدید:** در انتهای جدول می‌توانید گزینه `✏️ + ایجاد وضعیت سفارشی جدید...` را انتخاب کنید و نام دلخواهی مانند `در حال تست` یا `در حال بررسی` تایپ کنید.

---

### 🏷️ ۴.۷. فیلدهای کاستوم جیرا (Custom Fields Mapping)

جیرا برای فیلدهای اختصاصی شناسه `customfield_XXXXX` تولید می‌کند.

#### 🔍 نحوه پیدا کردن شناسه‌های کاستوم‌فیلد:
۱. روی دکمه **`🔍 پایش زنده API`** در بالای همین صفحه کلیک کنید.
۲. در انتهای گزارش، لیست تمام `customfield_XXXXX`های فعال رو تسک‌های جیرایتان نمایش داده می‌شود.

| نام فیلد | کلید در فایل `.env` | نحوه مقداردهی |
|----------|---------------------|----------------|
| **فیلد Sprint** | `JIRA_SPRINT_FIELD` | شناسه فیلد اسپرینت در جیرا (پیش‌فرض: `customfield_10020`). |
| **فیلد تیم منتظر** | `JIRA_WAITING_TEAM_FIELD` | شناسه فیلد کاستوم تیم معطل‌کننده (مثال: `customfield_10035`). |
| **فیلد دلیل انتظار** | `JIRA_WAITING_REASON_FIELD` | شناسه فیلد کاستوم علت انتظار (مثال: `customfield_10036`). |
| **فیلد لینک Confluence** | `JIRA_CONFLUENCE_LINK_FIELD` | شناسه فیلد لینک مستقیم مستندات (مثال: `customfield_10040`). |

---

### 📅 ۴.۸. نگاشت فیلدهای تاریخ (Date Mapping)

- `JIRA_EPIC_START_DATE_FIELD`: فیلد تاریخ شروع اپیک (پیش‌فرض: `created`).
- `JIRA_EPIC_DUE_DATE_FIELD`: فیلد تاریخ سررسید اپیک (پیش‌فرض: `duedate`).
- `JIRA_TASK_START_DATE_FIELD`: فیلد تاریخ شروع تسک.
- `JIRA_TASK_DUE_DATE_FIELD`: فیلد تاریخ سررسید تسک (پیش‌فرض: `duedate`).

---

### 🏷️ ۴.۹. پیشوند برچسب‌ها (Label Prefixes)

اگر از برچسب‌های تسک در جیرا استفاده می‌کنید، سیستم پیشوندهای زیر را می‌شناسد:
- `JIRA_WAIT_TEAM_PREFIX`: پیشوند تیم منتظر (پیش‌فرض: `wait:` ➔ برچسب: `wait:infra-team`).
- `JIRA_WAIT_REASON_PREFIX`: پیشوند دلیل انتظار (پیش‌فرض: `reason:` ➔ برچسب: `reason:approval`).
- `JIRA_CAPABILITY_PREFIX`: پیشوند قابلیت عملیاتی (پیش‌فرض: `cap:` ➔ برچسب: `cap:monitoring`).

---

## 5. پیکربندی کامل فایل `.env`

```env
# Server Config
PORT=3001
JWT_SECRET=ops-rd-secret-key-2026
SYNC_INTERVAL_MINUTES=60

# Jira Connection
JIRA_BASE_URL=https://aliamani6.atlassian.net
JIRA_USERNAME=aliamani66@gmail.com
JIRA_TOKEN=ATATT3xFfGF0RH_iF1oxCBw-0VCd_RE0eElRQk5sieUcx8zIFtFcgnQ7NE6IKC7vLIaDlQzxXHaq_HhDkdP4wShzcSG3VDx7PyRQiJihux0DA81SHnqgjY1naYlIUkXx3flrJirX0OtITzNty4oQAJ_aCeTWjAu3JC8OPcnYtNhkzQzdG4-xNgg=4444DE56
JIRA_PROJECT_KEY=ORD

# API Endpoints
JIRA_API_VERSION=auto
JIRA_SEARCH_ENDPOINT=/rest/api/3/search/jql
JIRA_PROJECT_ENDPOINT=/rest/api/3/project

# Statuses & Labels
JIRA_WAITING_STATUSES=OnHolding,Waiting,Blocked,On Hold
JIRA_WAIT_TEAM_PREFIX=wait:
JIRA_WAIT_REASON_PREFIX=reason:
JIRA_CAPABILITY_PREFIX=cap:

# Custom Fields
JIRA_SPRINT_FIELD=customfield_10020
JIRA_WAITING_TEAM_FIELD=customfield_10035
JIRA_WAITING_REASON_FIELD=customfield_10036
JIRA_CONFLUENCE_LINK_FIELD=customfield_10040

# Confluence Connection
CONFLUENCE_BASE_URL=https://aliamani6.atlassian.net/wiki
CONFLUENCE_USERNAME=aliamani66@gmail.com
CONFLUENCE_TOKEN=ATATT3xFfGF0RH_iF1oxCBw-0VCd_RE0eElRQk5sieUcx8zIFtFcgnQ7NE6IKC7vLIaDlQzxXHaq_HhDkdP4wShzcSG3VDx7PyRQiJihux0DA81SHnqgjY1naYlIUkXx3flrJirX0OtITzNty4oQAJ_aCeTWjAu3JC8OPcnYtNhkzQzdG4-xNgg=4444DE56
CONFLUENCE_DEFAULT_SPACE=OPS
```

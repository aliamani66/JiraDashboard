# No Approval Needed

- Never enter planning mode or create an implementation plan before making code changes.
- Never ask the user to approve changes before executing them.
- Make changes directly and immediately, then report what was done.
- Only pause if a change would be irreversible and destructive (e.g., deleting a database, removing a repo).
